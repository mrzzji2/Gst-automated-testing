# AI 写用例 - 在线开方专用模板

> 使用方法：复制下方模板，把【】里的内容替换成实际情况，发给 AI 即可。

---

## 第一步：让 AI 读项目背景（每次新对话开头必说）

```
请先阅读 CLAUDE.md，了解本项目的目录结构和用例编写规范，读完后告诉我你准备好了。
```

---

## 第二步：描述页面，让 AI 写用例

---

### 模板

```
我需要为【在线问诊 - 在线开方】页面编写自动化测试用例。

## 页面基本信息
- 页面路径：/home（在线开方为弹窗/对话框形式，在咨询列表页面内）
- 触发入口：左侧菜单 > 在线问诊 > 咨询列表 > 选择一个患者 > 点击右侧"在线开方"按钮
- 页面用途：医生为已选择的患者在线开具中药处方，含诊断、处方用药、剂量、药房选择、备注等

## 登录信息
- 登录 URL：https://doc-online-test.gstyun.cn/webClinic/index.html#/login
- 手机号输入框：`input[placeholder*='手机号']`
- 验证码输入框：`input[placeholder*='验证码']`（实际填密码）
- 登录按钮：`button:has-text('登录')`
- 医生选择：`radio`（选中医生）
- 确认切换按钮：`button:has-text('确认')` / `button:has-text('确认切换')`

## 页面有哪些内容

### 前置条件
- 已登录并进入医生工作台
- 左侧菜单 > 咨询列表（默认选中）
- 在患者列表中点击一个患者（如 test02）进入对话
- 点击右侧操作区的"在线开方"按钮

### 第一步：选择方案类型弹窗
点击"在线开方"后弹出 **"请选择方案类型"** 对话框，含 10 种处方类型：

| 处方类型 | 标签 |
|---------|------|
| 中药饮片 | 待发送 |
| 颗粒剂 | 待发送 |
| 定制丸剂 | 待发送 |
| 打粉散剂 | — |
| 滋补膏方 | — |
| 中西成药 | — |
| 经验方 | — |
| 外治项目 | — |
| 院内制剂 | — |
| 特色剂型 | — |

点击右上角 Close 按钮可关闭。

### 第二步：开方表单（以中药饮片为例）
选择类型后弹出完整的 **在线开方表单对话框**：

#### 顶部操作栏
- 展开病历（展开/收起患者病历资料）
- 导入问诊单
- 存为草稿
- 关闭按钮

#### 患者资料区（只读，自动带入）
- 姓名、性别、年龄

#### 诊断区（带 * 为必填）
- 主诉（多行文本输入框）*
- 现病史（多行文本输入框）*
- 中医诊断（标签形式，可删除，有输入框可新增）
- 中医证型（下拉选择）
- 舌苔面照（图片上传）
- 病历&检验检查（图片上传）

#### 处方用药区
- 药房标题：`中药饮片-药房名称`（如 天江药业颗粒剂(江阴)）
- 药房备注文字（如 1111111）
- 更换按钮（点击弹出选择方案类型及药房弹窗）
- 添加药材按钮（点击弹出选择药材弹窗）
- 共 N 味药材 / 总重 N g
- 存为常用方按钮
- 药材列表（每行包含）：
  - 药材名称（如 炒麦芽 6g）
  - 标签（如 省、国）
  - 用法（如 后下、另包冲服）
- 剂量设置：
  - 共 N 剂（spinbutton，范围 1-9999）
  - 每日 N 剂（spinbutton，范围 1-99）
  - 每日 N 次（spinbutton，范围 1-99）

#### 用药设置区
- 药材剂型：自煎（当前选中）
- 用药方式：内服 / 外用（radio 选择）
- 用药方法（下拉选择，选填）
- 服药忌口（标签形式，有输入框可新增）
- 医生嘱咐（多行文本，选填）
  - 常用医嘱按钮（弹出常用医嘱列表）
- 药房备注（多行文本，选填）
  - 常用备注按钮（弹出常用备注列表）

#### 底部
- 展开（展开/收起更多设置）
- 方案总计：X元
- 打印处方（checkbox）
- 确认发送按钮

### 第三步：更换药房弹窗
点击"更换"按钮弹出 **"选择方案类型及药房"** 对话框：

#### 左侧 — 处方类型选项卡
同选择方案类型弹窗的 10 种类型

#### 右侧 — 药房列表
药房按分类展示，每个药房包含：

| 信息项 | 说明 |
|--------|------|
| 药房名称 | 如 康美药业(广州) |
| 价格 | 如 21.62元 |
| 配送标签 | 如 同城配送、配送最快 |
| 剂型切换 | 自煎 / 代煎 |
| 单剂费用 | 单剂:X元 |
| 药费 | 药费:X元 |
| 加工费 | 加工费:X元 |
| 缺药提醒 | 如 缺药药材:炒麦芽 |

药房分类：默认药房、上次使用、常用药房、推荐药房、其他药房

### 第四步：添加药材弹窗
点击"添加药材"按钮弹出 **药材选择对话框**：

#### 顶部
- 当前选择药房信息（药房名、药材数、重量、单剂费用）
- 更换按钮
- 药材搜索框 + 添加按钮

#### 操作按钮
- 智能识方
- 缺药替换
- 清空药材

#### 已选药材列表
每行：药材名称 + 标签（省/国） + 重量（spinbutton） + 用法（下拉选择） + 单价

#### 底部
- 取消 / 完成按钮

#### 右侧 Tab
- 常用方 / 历史方 / 经典方

##### 常用方
- 搜索框 + 搜索按钮
- 分类筛选：全部、抑郁症、中药饮片、颗粒剂、定制丸剂、打散粉剂、定制膏方、中西成药、经验方、外治项目
- 常用方列表（每项含名称、药材组成、调用按钮）
- 管理按钮

##### 调用常用方后的确认弹窗
当已有药材时调用常用方，弹出：
> "定制方案内已添加药材，请问是替换现有药材还是在现有药材基础上添加药材？"
- 新增药材按钮
- 替换药方按钮

## 页面元素定位参考

```python
# ===== 选择方案类型弹窗 =====
BTN_ONLINE_PRESCRIBE = "text=在线开方"
PRESCRIPTION_TYPE_DIALOG = "[role='dialog']:has-text('请选择方案类型')"
PRESCRIPTION_TYPE_HERBAL = "[role='dialog']:has-text('请选择方案类型') >> text=中药饮片"
PRESCRIPTION_TYPE_GRANULE = "[role='dialog']:has-text('请选择方案类型') >> text=颗粒剂"
PRESCRIPTION_TYPE_PILL = "[role='dialog']:has-text('请选择方案类型') >> text=定制丸剂"
PRESCRIPTION_TYPE_CLOSE = "[role='dialog']:has-text('请选择方案类型') >> .el-dialog__headerbtn"

# ===== 开方表单 =====
PRESCRIPTION_FORM = "text=在线开方"
BTN_EXPAND_RECORD = "text=展开病历"
BTN_IMPORT_QUESTIONNAIRE = "text=导入问诊单"
BTN_SAVE_DRAFT = "[role='dialog']:has-text('在线开方') >> text=存为草稿"

INPUT_MAIN_COMPLAINT = "[placeholder='请填写主诉病史']"
INPUT_NOW_HISTORY = "[placeholder='请填写现病史']"
INPUT_DIAGNOSIS = "[placeholder='中医诊断']"
INPUT_SYNDROME = "[placeholder='请选择中医证型']"

# ===== 处方用药 =====
BTN_CHANGE_PHARMACY = "text=更换"
BTN_ADD_MEDICINE = "text=添加药材"
BTN_SAVE_COMMON = "text=存为常用方"
SPIN_TOTAL_DOSES = "spinbutton:has-text('共')"
SPIN_DAILY_DOSES = "spinbutton:has-text('每日')"
SPIN_DAILY_TIMES = "spinbutton:has-text('次')"

# ===== 用药设置 =====
RADIO_INTERNAL = "[role='dialog']:has-text('在线开方') >> text=内服"
RADIO_EXTERNAL = "[role='dialog']:has-text('在线开方') >> text=外用"
INPUT_MEDICATION_METHOD = "[placeholder='请填写（选填）']"
INPUT_DOCTOR_ADVICE = "[placeholder='请填写医生嘱咐（选填）']"
INPUT_PHARMACY_NOTE = "[placeholder='请填写药房备注（选填）']"
BTN_COMMON_ADVICE = "text=常用医嘱"
BTN_COMMON_NOTE = "text=常用备注"

# ===== 底部操作 =====
BTN_CONFIRM_SEND = "text=确认发送"
CHECKBOX_PRINT = "[role='dialog']:has-text('在线开方') >> text=打印处方"
TOTAL_PRICE = "text=方案总计"

# ===== 更换药房弹窗 =====
CHANGE_PHARMACY_DIALOG = ".el-dialog.pharmacys"

# ===== 添加药材弹窗 =====
ADD_MEDICINE_DIALOG = "[role='dialog']:has-text('当前选择药房')"
INPUT_MEDICINE_SEARCH = "[role='dialog']:has-text('当前选择药房') >> [placeholder='请输入药材名称']"
BTN_ADD = "button:has-text('添加')"
BTN_SMART_RECOGNIZE = "text=智能识方"
BTN_REPLACE_MISSING = "text=缺药替换"
BTN_CLEAR_ALL = "text=清空药材"
BTN_CANCEL = "button:has-text('取消')"
BTN_COMPLETE = "button:has-text('完成')"

# ===== 常用方（限定在添加药材弹窗内）=====
TAB_COMMON = "[role='dialog']:has-text('当前选择药房') >> text=常用方"
TAB_HISTORY = "[role='dialog']:has-text('当前选择药房') >> text=历史方"
TAB_CLASSIC = "[role='dialog']:has-text('当前选择药房') >> text=经典方"
INPUT_COMMON_SEARCH = "[role='dialog']:has-text('当前选择药房') >> [placeholder='输入常用方名称搜索']"
BTN_COMMON_SEARCH = "[role='dialog']:has-text('当前选择药房') >> button:has-text('搜索')"
BTN_CALL = "[role='dialog']:has-text('当前选择药房') >> button:has-text('调用')"

# ===== 调用确认弹窗 =====
CONFIRM_REPLACE_DIALOG = "text=替换现有药材"
BTN_ADD_NEW = "text=新增药材"
BTN_REPLACE = "text=替换药方"

# ===== 开方成功提示 =====
TOAST_SUCCESS = "text=开方成功"
```

## 需要验证的场景及预期结果

P0 冒烟（核心流程，必须通过）：
1. 打开在线开方弹窗
   - 前置：选择患者进入对话
   - 操作：点击"在线开方"按钮
   - 预期：弹出"请选择方案类型"对话框，显示 10 种处方类型
2. 选择处方类型并进入开方表单
   - 操作：在方案类型弹窗中点击一种类型（如"中药饮片"）
   - 预期：关闭类型选择弹窗，打开完整的在线开方表单，显示患者资料、诊断区、处方用药区、底部操作区

P1 回归（重要功能，回归必跑）：
3. 常用方搜索调用并提交处方（完整流程）
   - 前置：患者已有药材/处方记录（如 test02）
   - 操作：
     ① 选择患者 test02，点击"在线开方"
     ② 在方案类型弹窗中选择"中药饮片"
     ③ 在开方表单中点击"添加药材"
     ④ 在添加药材弹窗中切换到"常用方"Tab
     ⑤ 在搜索框输入关键词（如 regression），点击"搜索"
     ⑥ 在搜索结果中点击对应常用方的"调用"按钮
     ⑦ 弹窗提示"定制方案内已添加药材，请问是替换现有药材还是在现有药材基础上添加药材？"
     ⑧ 点击"替换药方"
     ⑨ 点击"完成"返回开方表单
     ⑩ 在药房备注输入框填入当前时间+文案（如 "2026-05-08 14:04 回归测试"）
     ⑪ 点击"确认发送"
   - 预期：
     - 搜索后返回匹配的常用方结果
     - 调用后弹出替换/新增确认弹窗
     - 点击替换药方后药材被替换为常用方内容（如山药6g、生地黄8g）
     - 点击完成返回表单，药材列表更新
     - 确认发送后提示"开方成功"
     - 聊天记录中生成新的处方单，药房备注可见
4. 主诉/现病史必填校验
   - 操作：不填写主诉或现病史，直接点击确认发送
   - 预期：提示必填项未填写，发送被阻止
5. 替换药方/新增药材确认
   - 操作：已有药材时调用新的常用方
   - 预期：弹出确认弹窗"替换现有药材还是在现有药材基础上添加"，可选新增药材或替换药方
6. 添加药材超剂量警告
   - 操作：在添加药材弹窗中输入药材"菊花"，设置用量1000g，点击添加
   - 预期：弹出警告弹窗"已超过建议使用最大量"，显示"更改药材剂量"按钮
7. 更换药房
   - 操作：在表单或添加药材弹窗中点击"更换"
   - 预期：弹出"选择方案类型及药房"弹窗，显示分类药房列表和价格信息
8. 存为草稿
   - 操作：填写部分信息后点击"存为草稿"
   - 预期：提示保存成功，草稿出现在草稿箱中
9. 用药方式切换
   - 操作：切换内服/外用 radio
   - 预期：选中状态正确切换
10. 清空药材
    - 操作：在添加药材弹窗点击"清空药材"
    - 预期：已选药材列表清空，药材数和重量归零
11. 药材剂量调整
    - 操作：在添加药材弹窗中修改药材重量
    - 预期：单剂费用和方案总计随之更新

P2（边缘场景）：
12. 关闭方案类型弹窗
    - 操作：点击弹窗的 Close 按钮
    - 预期：弹窗关闭，回到咨询列表页面
13. 搜索不存在的药材
    - 操作：在添加药材搜索框输入不存在的药材名
    - 预期：列表显示"暂无数据"或空状态
14. 搜索不存在的常用方
    - 操作：在常用方搜索框输入不存在的名称
    - 预期：列表显示"没有更多了"，无匹配结果
15. 勾选/取消打印处方
    - 操作：点击打印处方 checkbox
    - 预期：勾选状态正确切换
16. 不同处方类型切换
    - 操作：在更换药房弹窗中切换不同类型 Tab
    - 预期：药房列表按类型刷新
17. 用药方法下拉选择
    - 操作：点击用药方法下拉框
    - 预期：弹出可选选项供选择
18. 滋补膏方缺少辅料校验
    - 前置：选择患者 test02
    - 操作：
      ① 点击在线开方，选择"滋补膏方"
      ② 点击添加药材 → 清空药材
      ③ 添加"山药"4g 和"生地黄"4g
      ④ 点击完成 → 确认发送
    - 预期：弹出提示"膏方必须添加辅料"

## 备注
- 在线开方为弹窗/对话框形式，不涉及页面跳转，URL 不变
- 前置条件：必须先选择患者（有问诊记录的患者）才能点击在线开方
- 弹窗使用 Element UI 组件，打开/关闭时有动画过渡（`dialog-fade-enter-active`），操作弹窗内的元素前建议等待动画完成，必要时可等待 `.el-loading-mask` 消失后再操作
- 选择器注意事项：
  - `<input>` 和 `<textarea>` 的 placeholder 文本使用 `[placeholder='xxx']` 定位，`textbox:has-text()` 对其无效
  - 多弹窗嵌套时（如方案类型弹窗内的类型选择），必须用 `[role='dialog']:has-text('xxx') >>` 限定作用域，避免匹配到页面其他区域的同名文本
  - 关闭弹窗推荐使用 `.el-dialog__headerbtn` 定位关闭按钮（最可靠），或使用 `Escape` 键（需确保焦点在弹窗内）
  - 检测弹窗是否可见时需注意：Element UI 的嵌套弹窗结构中，`wait_for_selector(state="visible")` 可能检测失败，可使用 `state="attached"` 判断 DOM 存在
- 不同处方类型（中药饮片、颗粒剂等）的表单字段可能有差异，用例建议以中药饮片为主覆盖
- 药房列表数据动态变化（价格、库存、配送信息），断言时注意使用模糊匹配
- 处方提交后不可撤回，测提交时建议使用 test02 等测试患者
- 常用方数据为测试环境数据，各环境可能不同
- Page Object 类建议在 `pages/online_consultation_page.py` 中扩展，或在 `pages/` 下新建 `prescription_page.py`
- 元素定位器建议在 `elements/online_consultation_locators.py` 中扩展，或在 `elements/` 下新建 `prescription_locators.py`
- 测试类用 `@pytest.mark.page("医生工作台-在线问诊-在线开方")`
```

---

## 填写说明

| 字段 | 怎么填 |
|------|--------|
| 页面路径 | 在线开方为弹窗，主页面路径为 `/home` |
| 登录信息 | 页面需要登录态，使用 conftest.py 的 fixture 自动处理 |
| 页面内容 | 截图发给 AI，或参照模板中的内容描述（选择方案类型→开方表单→更换药房→添加药材→常用方） |
| P0 场景 | 开方的核心流程——打开弹窗、选类型、填表单、提交 |
| P1 场景 | 重要功能——常用方调用、药房更换、存草稿、剂量调整、用药切换 |
| P2 场景 | 边缘情况——关闭弹窗、搜索无结果、空数据校验、不同类型切换 |
| 预期结果 | 操作完之后，页面上应该出现什么 / 消失什么 / 弹窗如何变化 |

---

## 注意事项

每个测试类**必须**加 `@pytest.mark.page("页面中文名")`，失败通知会用这个名称标注是哪个页面出问题，例如：

```python
@allure.feature("医生工作台")
@allure.story("在线问诊 - 在线开方")
@pytest.mark.page("医生工作台-在线问诊-在线开方")      # ← 必填，通知分类用
class TestOnlinePrescription:
    ...
```

---

## 常用接口

### 核心接口（预估，具体需抓包确认）
- 获取处方类型列表：`POST /webApi/v1/prescription/getTypes`
- 获取药房列表：`POST /webApi/v1/pharmacy/list`
- 搜索药材：`POST /webApi/v1/medicine/search`
- 获取常用方：`POST /webApi/v1/prescription/getCommon`
- 提交处方：`POST /webApi/v1/prescription/create`
- 保存草稿：`POST /webApi/v1/prescription/saveDraft`
