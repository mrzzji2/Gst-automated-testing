# AI 写用例 - 在线问诊专用模板

> 使用方法：复制下方模板，把【】里的内容替换成实际情况，发给 AI 即可。

---

## 第一步：让 AI 读项目背景（每次新对话开头必说）

```
请先阅读 CLAUDE.md，了解本项目的目录结构和用例编写规范，读完后告诉我你准备好了。
```

---

## 第二步：描述页面，让 AI 写用例

---

### 模板

```
我需要为【在线问诊 - 咨询列表】页面编写自动化测试用例。

## 页面基本信息
- 页面路径：/home
- 菜单位置：左侧菜单 > 在线问诊 > 咨询列表
- 页面用途：医生查看患者的咨询消息，与患者进行在线对话沟通，发送问诊单、在线开方、结束问诊等

## 登录信息
- 登录 URL：https://doc-online-test.gstyun.cn/webClinic/index.html#/login
- 手机号输入框：`input[placeholder*='手机号']`
- 验证码输入框：`input[placeholder*='验证码']`（实际填密码）
- 登录按钮：`button:has-text('登录')`
- 医生选择：`radio`（选中医生）
- 确认切换按钮：`button:has-text('确认')` / `button:has-text('确认切换')`

## 页面有哪些内容

### 顶部导航
- AI诊室
- 医生工作台
- 患教文章
- 医案分享
- 问诊单

### 左侧功能菜单（共10项）
- 在线问诊
- 门店出诊
- 全部处方
- 常用方
- 咨询列表（默认选中，默认进入页面）
- 今日扫码
- 全部患者
- 电话看诊（带数字角标，显示待处理数量）
- 视频看诊（带数字角标，显示待处理数量）
- 门诊预约

### 中间内容区 - 患者消息列表
- 搜索框：`input[placeholder*='患者']`，输入患者姓名搜索
- 患者消息列表（`#consultList li`），每项包含：
  - 患者头像
  - 患者姓名（如：test02、小固助理）
  - 最后消息时间
  - 最后消息内容预览
  - 状态标签：已结束、填写问诊单、共 N 题 等

### 右侧操作区（选中患者后显示）
#### 快捷操作按钮
- 快捷回复
- 发问诊单
- 在线开方（点击弹出选择方案类型弹窗，含多种处方类型）
- 视频看诊（有二级菜单：视频看诊 / 语音问诊）
- 电话看诊（有二级菜单：电话问诊 / 语音问诊）
- 结束问诊（弹出确认弹窗：确认结束 / 取消 / 退号退款）
- 病历管理

#### 患者详情面板
- 姓名、性别、年龄
- 身高、体重
- 微信、电话
- 问诊单卡片
- 历史处方

#### 对话区域
- 聊天消息记录
- 消息输入框：`div:has-text('请输入内容')` 或 `[contenteditable='true']`
- 发送按钮：`button:has-text('发送')`
- 提示文字：回车发送/Ctrl+Enter换行

#### 草稿箱区域
- 草稿箱标题
- 草稿数量显示
- 草稿条目列表

## 页面元素定位参考

```python
# ===== 顶部导航 =====
NAV_AI_CLINIC = "a:has-text('AI诊室')"
NAV_DOCTOR_WORKBENCH = "a:has-text('医生工作台')"
NAV_HEALTH_EDUCATION = "a:has-text('患教文章')"
NAV_MEDICAL_CASE = "a:has-text('医案分享')"
NAV_QUESTIONNAIRE = "a:has-text('问诊单')"

# ===== 左侧菜单 =====
MENU_ONLINE_CONSULTATION = "text=在线问诊"
MENU_STORE_VISIT = "text=门店出诊"
MENU_ALL_PRESCRIPTIONS = "text=全部处方"
MENU_COMMON_PRESCRIPTIONS = "text=常用方"
MENU_CONSULTATION_LIST = "text=咨询列表"
MENU_TODAY_SCAN = "text=今日扫码"
MENU_ALL_PATIENTS = "text=全部患者"

# ===== 患者搜索与列表 =====
PATIENT_SEARCH_INPUT = "input[placeholder*='患者' i]"
PATIENT_LIST = "#consultList"
PATIENT_LIST_ITEM = "#consultList li"
PATIENT_NAME = ".name"
STATUS_ENDED = "text=已结束"

# ===== 右侧操作按钮 =====
BUTTON_QUICK_REPLY = "text=快捷回复"
BUTTON_SEND_QUESTIONNAIRE = "text=发问诊单"
BUTTON_ONLINE_PRESCRIBE = "text=在线开方"
BUTTON_VIDEO_CONSULTATION = "text=视频看诊"
BUTTON_PHONE_CONSULTATION = "text=电话看诊"
BUTTON_END_CONSULTATION = "text=结束问诊"
BUTTON_MEDICAL_RECORD = "text=病历管理"

# ===== 消息输入 =====
MESSAGE_INPUT = "div:has-text('请输入内容'), [contenteditable='true']"
BUTTON_SEND = "button:has-text('发送')"

# ===== 结束问诊弹窗 =====
END_CONSULTATION_DIALOG = ".dialog:has-text('结束本次咨询')"
BUTTON_CONFIRM_END = "button:has-text('确认结束')"
BUTTON_CANCEL_END = "button:has-text('取消')"
BUTTON_REFUND_END = "button:has-text('退号退款')"

# ===== 开方弹窗 =====
PRESCRIPTION_MODAL = ".dialog:has-text('请选择方案类型')"

# ===== 草稿箱 =====
DRAFT_BOX_HEADING = "heading:has-text('草稿箱')"
DRAFT_ITEM = ".draft-item"

# ===== 患者详情 =====
PATIENT_INFO_PANEL = "text=电话:"
PATIENT_GENDER = "text=男, text=女"
```

## 需要验证的场景及预期结果

P0 冒烟（核心流程，必须通过）：
1. 验证页面基本元素
   - 预期：患者列表可见且数量 > 0，搜索框可用
2. 选择患者进入会话
   - 预期：患者详情面板显示，显示患者姓名、性别、联系方式等信息
3. 发送文本消息
   - 预期：消息发送成功，输入框清空
4. 按回车键发送消息
   - 预期：按回车后消息发送成功

P1 回归（重要功能，回归必跑）：
5. 验证左侧功能菜单项
   - 预期：菜单项完整，名称正确
6. 验证电话看诊和视频看诊的数字角标
   - 预期：显示具体数字，角标数据格式正确
7. 按患者名称搜索
   - 预期：列表只展示匹配的患者
8. 清空搜索条件
   - 预期：恢复显示完整患者列表
9. 点击在线开方按钮
   - 预期：弹出选择方案类型弹窗，可选择关闭
10. 点击发问诊单按钮
    - 预期：弹出问诊单发送界面
11. 点击快捷回复按钮
    - 预期：弹出快捷回复内容供选择
12. 点击结束问诊按钮
    - 预期：弹出结束问诊确认弹窗（含确认结束、取消、退号退款按钮）
13. 检查草稿箱显示
    - 预期：草稿箱区域显示标题和数量
14. 查看患者详情信息
    - 预期：显示患者姓名、性别、年龄、电话等信息

P2（边缘场景）：
15. 搜索不存在的患者
    - 预期：列表显示"暂无数据"或空状态
16. 发送空消息（不输入内容直接点击发送）
    - 预期：提示"请输入内容"或发送按钮不响应
17. 取消结束问诊
    - 预期：点击取消后弹窗关闭，问诊状态不变
18. 快速连续点击开方按钮
    - 预期：只打开一个开方弹窗，不会重复弹出
19. 按 ESC 键关闭弹窗
    - 预期：弹窗关闭，页面状态不变
20. 已结束问诊的患者点击结束问诊
    - 预期：仍然弹出结束问诊确认弹窗

## 备注
- 页面数据依赖登录态，使用 conftest.py 中的 gst_online_consultation_page fixture（已自动登录并导航到在线问诊页面）
- 测试前需要选择正确的医生账号
- 患者数据是动态的，断言时注意使用模糊匹配或只验证结构存在
- 数字角标（电话看诊、视频看诊）的数据可能实时变化，断言时验证是数字即可
- 消息相关测试建议在消息内容后添加时间戳避免重复
- Page Object 类为 `OnlineConsultationPage`，位于 `pages/online_consultation_page.py`
- 元素定位器类为 `OnlineConsultationLocators`，位于 `elements/online_consultation_locators.py`
- 测试类统一用 `@pytest.mark.page("医生工作台-在线问诊")`
```

---

## 填写说明

| 字段 | 怎么填 |
|------|--------|
| 页面路径 | 浏览器地址栏里 `#/` 后面的部分，在线问诊为 `/home` |
| 登录信息 | 如果页面需要登录，提供完整的登录流程和接口信息 |
| 页面内容 | 截图发给 AI，或者照着页面逐一描述 |
| P0 场景 | 页面最核心的功能——查看患者列表、选择患者、发送消息 |
| P1 场景 | 重要功能——菜单验证、搜索、开方、结束问诊、查看详情 |
| P2 场景 | 边缘情况——空数据、取消操作、快速点击、空消息 |
| 预期结果 | 操作完之后，页面上应该出现什么 / 消失什么 / 跳转到哪里 |

---

## 注意事项

每个测试类**必须**加 `@pytest.mark.page("页面中文名")`，失败通知会用这个名称标注是哪个页面出问题，例如：

```python
@allure.feature("医生工作台")
@allure.story("在线问诊")
@pytest.mark.page("医生工作台-在线问诊")      # ← 必填，通知分类用
class TestOnlineConsultation:
    ...
```

---

## 常用页面路径和接口

### 页面路径
- 登录页：`/login`
- 工作台（首页）：`/home`

### 页面跳转流程
1. 访问首页 → 自动跳转到登录页
2. 输入手机号和验证码 → 点击登录
3. 选择医生账号 → 点击确认切换
4. 进入工作台 → 默认显示咨询列表

### 核心接口
- 登录：`POST /webApi/v1/auth/login`
- 获取医生信息：`POST /webApi/v1/doctor/getDoctor`
- 获取登录账号列表：`POST /webApi/v1/doctorSubAccount/getLoginAccounts`
- 获取选项配置：`POST /webApi/v1/auth/getOptions`
